{
	"name": "無題",
	"version": "0.0.10",
	"private": true,
	"type": "module",
	"author": {
		"name": "Kristalli",
		"email": "kristal.dev@pm.me"
	},
	"license": "The Unlicense",
	"scripts": {
		"changelog": "echo it's 'pnpm changeset' :D",
		"dev": "vite dev",
		"build": "vite build",
		"build:dev": "pnpm format && pnpm lint && pnpm build",
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"lint": "prettier --plugin-search-dir . --check .",
		"format": "prettier --plugin-search-dir . --write ."
	},
	"devDependencies": {
		"@changesets/cli": "^2.26.0",
		"@sveltejs/adapter-auto": "^1.0.0",
		"@sveltejs/kit": "^1.0.0",
		"dotenv": "^16.0.3",
		"prettier": "^2.8.0",
		"prettier-plugin-svelte": "^2.8.1",
		"svelte": "^3.54.0",
		"svelte-check": "^2.9.2",
		"tslib": "^2.4.1",
		"typescript": "^4.9.3",
		"vite": "^4.0.0"
	}
}
